DIGEST:
[1] t+0ms kind=app_switch: Switched to Chrome
[2] t+3400ms kind=click: Clicked button labeled "Compose" in Chrome
[3] t+6800ms kind=text_input: Typed "team@acme.com" in Chrome
[4] t+9200ms kind=text_input: Typed "Weekly update" in Chrome
[5] t+18400ms kind=click: Clicked button labeled "Send" in Chrome

OUTPUT:
{
  "markdown": "# Send weekly update email\n\nOpens Gmail in Chrome, composes a new message to the team distribution list, and sends it with a subject line.\n\n## Parameters\n\n- `recipient` (string, required) — the destination email address\n- `subject` (string, required) — the email subject line\n\n## Preconditions\n\n- Chrome is installed and Gmail is already signed in\n\n## Steps\n\n1. Switch focus to Chrome with the Gmail inbox tab visible (screenshot 1).\n2. Click the red **Compose** button in the top-left of the Gmail sidebar (screenshot 2).\n3. Type {recipient} into the **To** field at the top of the compose pane (screenshot 3).\n4. Type {subject} into the **Subject** field directly below the To field (screenshot 4).\n5. ⚠️ [DESTRUCTIVE] Click the blue **Send** button at the bottom-left of the compose pane to send the email (screenshot 5).\n\n## Expected outcome\n\nThe email is delivered to {recipient} with the specified subject line.\n",
  "meta": {
    "slug": "send_weekly_update",
    "name": "Send weekly update email",
    "trajectory_id": "00000000-0000-0000-0000-000000000000",
    "created_at": "2026-01-01T00:00:00+00:00",
    "parameters": [
      {"name": "recipient", "type": "string", "required": true},
      {"name": "subject", "type": "string", "required": true}
    ],
    "destructive_steps": [5],
    "preconditions": ["Chrome is installed and Gmail is already signed in"],
    "step_count": 5,
    "steps": [
      {
        "number": 2,
        "intent": "open_compose",
        "screenshot_ref": "0002.png",
        "execution_hints": [
          {"tier": "computer_use", "summary": "Click the red Compose button in the top-left of the Gmail sidebar."}
        ]
      },
      {
        "number": 5,
        "intent": "send_email",
        "screenshot_ref": "0005.png",
        "execution_hints": [
          {"tier": "computer_use", "summary": "Click the blue Send button at the bottom-left of the compose pane."}
        ]
      }
    ]
  },
  "questions": [
    {"id": "q1", "category": "parameterization", "text": "Should the recipient always be team@acme.com, or does it change between runs?"},
    {"id": "q2", "category": "destructive", "text": "Should the Runner always pause before clicking Send, or should it auto-send once on an approved skill?"}
  ]
}
