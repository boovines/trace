[project]
name = "trace-recorder"
version = "0.1.0"
description = "Trace Module 1: captures macOS user actions into a trajectory."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "pydantic>=2.8",
]
