# Ralph Progress Log
Started: Wed Apr 22 15:33:38 EDT 2026

## Codebase Patterns
- `services/recorder` uses a **src layout** (`services/recorder/src/recorder/…`); future files (schema.py, writer.py, session.py, etc.) go under `src/recorder/`.
- The recorder package is installed as a uv workspace member (`trace-recorder`) and should be imported as `from recorder.xyz import …`, NOT `services.recorder.recorder.xyz`.
- Root `pyproject.toml` owns shared tooling config (ruff/mypy/pytest); per-service `pyproject.toml` only declares that service's runtime deps + hatchling build.
- Ruff's isort needs `known-first-party = ["recorder", "synthesizer", "runner", "gateway"]` or it misgroups the workspace imports in gateway/main.py.
- PyObjC wheels only exist for macOS — deps are marked `; sys_platform == 'darwin'` so `uv sync` still works if Ralph ever runs on non-darwin.
- `mypy --strict` has PyObjC module overrides in root pyproject (`Cocoa.*`, `Quartz.*`, `ApplicationServices.*`, `AppKit.*`, `Foundation.*`, `objc.*`); add new PyObjC module imports here, not in each file.
- Quality-gate commands for the recorder module are:
  - `uv run ruff check services/recorder`
  - `uv run mypy --strict services/recorder`
  - `uv run pytest tests/recorder/`
- Pre-existing gateway/integration test failures (`ModuleNotFoundError: gateway`) are NOT caused by R-001 and are outside this branch's scope — a future `feat/integration` story will install gateway as a proper package.
- **Schema validation**: `recorder.schema` exposes `validate_event(event)` / `validate_metadata(meta)` backed by `jsonschema`'s Draft 2020-12 validator (with `FORMAT_CHECKER` enabled for `uuid`/`date-time`). Future writer/session code should call these before writing JSON to disk so bad events never land in `events.jsonl`.
- **Schema file lookup**: `recorder.schema.find_schema_path()` walks up from `__file__` to find `contracts/trajectory.schema.json`. Works for the uv-workspace editable install. If the contracts ever move or get bundled into the wheel, update this function — do NOT hardcode a path.
- **Local `$ref` only**: the `load_event_schema()` / `load_metadata_schema()` wrappers build standalone schemas with a `#/$defs/...` fragment ref (no `$id`), which keeps `jsonschema` from trying to HTTP-fetch the schema's `$id` URL at validate time. If you add new top-level sub-schemas for Synthesizer/Runner to reuse, follow the same pattern.
- **TrajectoryWriter ownership**: `recorder.writer.TrajectoryWriter` is the only module allowed to mutate the on-disk trajectory directory. All writes go through its lock; session/orchestrator code must not write `events.jsonl` / `metadata.json` / `screenshots/*` directly. It validates via `recorder.schema.validate_{event,metadata}` BEFORE any disk write, so bad data never leaks to disk. The constructor creates `<root>/<uuid>/` + `screenshots/` with perms `0700` and the writer is usable as a context manager (`__exit__` calls `close()`).
- **Atomic JSON writes**: `_atomic_write_json(path, data)` writes to `<path>.tmp`, fsyncs, and renames. Use it for any metadata-style file. `events.jsonl` is append-flush (no fsync per line — documented v1 tradeoff).
- **Datetime import style**: use `from datetime import UTC, datetime` (Python 3.11+). Ruff's `UP017` flags `timezone.utc` — don't revive it. ISO strings are produced with `datetime.now(UTC).isoformat(timespec="milliseconds")` which yields `+00:00` suffixes; the schema's `format: date-time` accepts both `+00:00` and `Z`.
- **Ruff SIM105**: prefer `with contextlib.suppress(OSError): os.fsync(...)` over `try/except/pass` for best-effort fsync on filesystems that don't support it (tmpfs in pytest tmp_path).
- **PyObjC mocking in tests**: for any module that does `from ApplicationServices import X` / `from Quartz import Y` inside a function, tests stub the framework by installing a `types.ModuleType("Quartz")` / `types.ModuleType("ApplicationServices")` into `sys.modules` via `monkeypatch.setitem`. The import inside the tested function then resolves to the stub. For ImportError coverage, install a stub that is missing the expected attributes — `from X import missing_attr` raises ImportError, which the module under test should catch.
- **Permission-check pattern**: `recorder.permissions` keeps each macOS framework call in its own thin `_check_*` helper, and `check_permissions()` / `get_missing_permissions_error()` simply compose them. Tests patch the helpers for combination coverage and patch the PyObjC symbols inside `sys.modules` for helper-level coverage. Use the same pattern for any future PyObjC-backed module (event_tap, ax_resolver, focus_tracker, screenshot) to keep the suite hermetic on Ralph's sandbox.
- **`@pytest.mark.macos` is opt-in only**: `tests/recorder/conftest.py` auto-skips every test tagged `macos` unless `TRACE_RUN_MACOS_TESTS=1` is set. Use this marker for ANY test that needs real PyObjC framework behaviour (CGEventTap, AXUIElement, CGWindowListCreateImage, etc.). Hermetic unit coverage should still be provided alongside via `sys.modules`-stubbed Quartz/ApplicationServices so the default run stays green.
- **Splitting PyObjC callback from normaliser**: for modules driven by a C-callback (CGEventTap), put the pure event→dict conversion in a module-level function that takes the framework module as a parameter (`_normalise_event(code, event, quartz)`), and expose a thin `handle_cg_event(code, event)` method on the class that the tap callback delegates to. Tests exercise the normaliser + re-enable logic by calling `handle_cg_event` with fake events — no run-loop or thread needed. `event_tap.py` is the reference implementation.
- **Re-enabling a disabled tap**: `CGEventTapEnable(tap, True)` re-arms a tap the OS disabled via `kCGEventTapDisabledByTimeout` / `kCGEventTapDisabledByUserInput`. Both constants appear as unsigned `uint32` (0xFFFFFFFE / 0xFFFFFFFF) in headers but PyObjC sometimes surfaces them as signed ints (-2 / -1); match both forms.
- **Bounded-time framework calls**: when a PyObjC call could block indefinitely (AX queries, inter-app RPC), wrap it with a daemon `threading.Thread` and a `threading.Event` set in a `finally`. The caller `event.wait(timeout)` — on timeout the worker thread just keeps living as a daemon. Do not rely on `Thread.join(timeout)` alone to tell "done" from "still running"; always use a separate event sentinel. See `ax_resolver.resolve_element_at` for the reference.
- **PyObjC AX value shapes**: AX position/size attributes come back as one of three shapes depending on macOS/PyObjC version — native 2-tuple, bridged object with `.x/.y` or `.width/.height`, or raw `AXValueRef` that must be unwrapped via `AXValueGetValue(value, kAXValueCG{Point,Size}Type, None) -> (success, unpacked)`. Any new AX-consuming code should tolerate all three (see `_unpack_ax_value` in ax_resolver.py).
- **NSWorkspace subscription pattern**: prefer `addObserverForName:object:queue:usingBlock:` (a Python callable passed as block) over the NSObject-subclass selector pattern — it keeps the handler method on the Python class and avoids `@objc.selector` machinery. The block returns an opaque observer handle that must be passed back to `removeObserver:` on stop. Tests stub `AppKit.NSWorkspace.sharedWorkspace().notificationCenter()` with a class that captures `(name, block)` in a list so we can invoke the block synchronously with a fake notification. See `focus_tracker._subscribe_workspace` for the reference.
- **NSDictionary dual-access**: `userInfo()` on an `NSNotification` is bridged to a Python dict in most PyObjC builds but not all. Accept both by trying `.get(key)` first (callable on a bridged dict) and falling back to `objectForKey_(key)` on an ObjC-only stub. Same pattern applies to any NSDictionary the recorder reads out of a framework callback.

---

## 2026-04-22 - R-001
- Moved `services/recorder/recorder/` → `services/recorder/src/recorder/` (src layout per AC).
- Added `services/recorder/src/recorder/__main__.py` for standalone `python -m recorder` dev runs.
- Rewrote `services/recorder/pyproject.toml`: added fastapi, uvicorn, pydantic, jsonschema, PyObjC (darwin-gated) deps; added hatchling build backend pointing at `src/recorder`.
- Updated root `pyproject.toml`: added `trace-recorder` workspace source, dev deps (pytest, pytest-asyncio, httpx, mypy, ruff, types-jsonschema), ruff known-first-party config, mypy_path for recorder src, broader PyObjC mypy overrides.
- Updated `gateway/main.py` to import `from recorder.api import router` (installed package, not filesystem path).
- Replaced `tests/recorder/.gitkeep` with `__init__.py` + `conftest.py` + `test_scaffold.py` placeholder that asserts `recorder.__version__`.
- Files changed:
  - `pyproject.toml`
  - `services/recorder/pyproject.toml`
  - `services/recorder/src/recorder/{__init__.py,__main__.py,api.py}` (new)
  - `services/recorder/recorder/{__init__.py,api.py}` (deleted)
  - `gateway/main.py`
  - `tests/recorder/{__init__.py,conftest.py,test_scaffold.py}` (new)
  - `tests/recorder/.gitkeep` (deleted)
- Quality gates: ruff/mypy/pytest all green on services/recorder + tests/recorder; full-repo ruff also clean.
- **Learnings for future iterations:**
  - `uv sync --extra dev` is the command to pull dev tooling; plain `uv sync` skips it.
  - `pytest` returns exit code 5 on zero-collection — the AC wanted exit 0, so added a placeholder test. Future stories can keep or replace it; don't accidentally regress to zero tests.
  - `hatchling` with `packages = ["src/recorder"]` gives a clean editable install; no need for setuptools.
  - When adding a new PyObjC import (e.g. `import Quartz` in R-005), also ensure the module is listed in the mypy override block in root pyproject.toml, otherwise `mypy --strict` will fail on `Any`-returning calls.
  - Gateway already imports synthesizer/runner via `services.X.X.api` — keep those imports intact; only `recorder` moved to installed-package style in R-001.
---

## 2026-04-22 - R-002
- Rewrote `contracts/trajectory.schema.json` as the locked R-002 contract: event shape `{seq, timestamp_ms, type, screenshot_ref, app, target, payload}` with per-type payload subschemas (click, keypress, scroll, app_switch, window_focus, text_input, keyframe, tap_reenabled) discriminated via `allOf` + `oneOf` on the `type` const. Metadata shape updated to `{id, label, started_at, stopped_at, display_info{width,height,scale_factor}, app_focus_history[{bundle_id,name,entered_at,exited_at}]}`.
- Added `services/recorder/src/recorder/schema.py` with `find_schema_path`, `load_event_schema`, `load_metadata_schema`, `validate_event`, `validate_metadata` (Draft202012Validator + FORMAT_CHECKER for uuid/date-time).
- Added `tests/recorder/test_schema.py` covering 8 valid events (one per type), 10 malformed events (missing required fields, unknown enum values, bad `screenshot_ref` pattern, extra top-level keys, `target` missing `frame`, seq=0, empty text_input), plus metadata happy-path / null-stopped_at / bad-uuid cases.
- Files changed:
  - `contracts/trajectory.schema.json` (rewritten)
  - `services/recorder/src/recorder/schema.py` (new)
  - `tests/recorder/test_schema.py` (new)
- Quality gates: `uv run ruff check services/recorder tests/recorder`, `uv run mypy --strict services/recorder`, `uv run pytest tests/recorder/` — all pass (24 tests).
- **Learnings for future iterations:**
  - `jsonschema.Draft202012Validator` does NOT validate `format` keywords by default — pass `format_checker=Draft202012Validator.FORMAT_CHECKER` to enforce `uuid`/`date-time`. Without it, `id: "not-a-uuid"` silently passes.
  - If a sub-schema you build at runtime has an `$id` pointing at a non-resolvable URL (e.g. `https://trace.local/...`) and a `$ref` of the same URL, the `referencing` resolver will try to HTTP-fetch it and explode with `URLError`. Use a bare `#/$defs/...` fragment ref (no `$id`) to keep resolution purely local.
  - For `oneOf` failures, the top-level `ValidationError.message` is the generic "not valid under any of the given schemas" — the specific field-level errors live in `err.context`. Tests asserting a specific needle must walk `.context` recursively (see `_collect_error_text` in `test_schema.py`).
  - When Ralph adds R-003's writer, it should call `schema.validate_event` BEFORE `events.jsonl.write(...)` — that's the cheapest way to make "writing schema-invalid data" impossible at the module boundary.
---

## 2026-04-22 - R-003
- Added `services/recorder/src/recorder/writer.py` exposing `TrajectoryWriter` with constructor that mints a uuid, creates `<root>/<uuid>/` + `screenshots/` at perms `0700`, and opens `events.jsonl` for append.
- `write_metadata(metadata)` validates via `recorder.schema.validate_metadata`, then writes atomically (`.tmp` → fsync → rename) and caches the dict so `close()` can finalise `stopped_at`.
- `append_event(event)` assigns `seq` if missing (or `None`), validates, JSON-encodes one line, flushes. A single `threading.Lock` guards seq assignment + file write so the CGEventTap background thread and the main-thread text aggregator can share a writer. The counter is kept strictly ahead of any caller-supplied seq so auto-assigned seqs never collide with pre-assigned ones.
- `write_screenshot(seq, png_bytes)` writes `screenshots/NNNN.png` (4-digit zero-padded, extends past 4 digits for large seqs — the schema pattern allows `\d{4,}`).
- `close()` flushes the events handle, sets `stopped_at` on metadata (reading from disk if the cached copy is absent), re-validates, and rewrites atomically. Idempotent — second `close()` is a no-op — and further `append_event` raises `RuntimeError`.
- Also added `__enter__`/`__exit__` so the writer can be used as a context manager.
- `tests/recorder/test_writer.py` (11 tests): directory layout + perms, duplicate-id guard, round-trip with 10 events + 3 screenshots, bad-payload rejection without disk leak, bad metadata rejection, 5×20 concurrent append with distinct monotonic seq, caller-supplied seq preservation + counter advance, idempotent close, zero-padded filename for small and 5-digit seqs, close-without-metadata safety, context-manager exit.
- Files changed:
  - `services/recorder/src/recorder/writer.py` (new)
  - `tests/recorder/test_writer.py` (new)
  - `scripts/ralph/prd.json` (R-003 passes=true)
- Quality gates: `uv run ruff check services/recorder tests/recorder`, `uv run mypy --strict services/recorder`, `uv run pytest tests/recorder/` — all pass (35 tests total).
- **Learnings for future iterations:**
  - `datetime.now(timezone.utc)` trips ruff `UP017` on Python 3.11+ — use `from datetime import UTC` and `datetime.now(UTC)`.
  - `tmp.replace(final)` on POSIX is atomic and works cross-device on the same filesystem; it's the right primitive for "write then swap into place".
  - `Path.with_suffix(".png.tmp")` is wrong for multi-dot suffixes (replaces the existing `.png`). Use `path.with_name(path.name + ".tmp")` to append a suffix safely.
  - When assigning `seq` before validating, copy the dict (`dict(event)`) so validation failures don't mutate the caller's input — this matters for the session orchestrator that may retry or re-route a rejected event.
  - The writer's `_lock` is coarse (guards both the counter and the file handle). That's fine for the expected event rate (well under 1k events/sec); if we ever need finer granularity, split into a `seq_lock` + `write_lock` — but beware of ordering: the on-disk order must match seq order for tests and for downstream Synthesizer consumption.
  - `close()` reading the metadata from disk as a fallback means that even if a test calls `close()` without ever calling `write_metadata()`, we don't crash — we just skip the `stopped_at` finalisation. The orchestrator should always call `write_metadata()` first, but the writer defends against the bad ordering.
---

## 2026-04-22 - R-004
- Added `services/recorder/src/recorder/permissions.py` exposing `check_permissions()` and `get_missing_permissions_error()`.
  - `_check_accessibility()` calls `AXIsProcessTrustedWithOptions({kAXTrustedCheckOptionPrompt: False})` — pure inspection, never prompts (the Tauri UI owns prompting UX per the PRD).
  - `_check_screen_recording()` prefers `CGPreflightScreenCaptureAccess()` and falls back to `CGDisplayCreateImageForRect` on a 1x1 rect when preflight is missing or raises.
  - `_check_input_monitoring()` attempts a listen-only `CGEventTapCreate` with an empty mask and treats a non-NULL return as proof of permission; the probe tap is invalidated immediately via `CFMachPortInvalidate` so no run-loop source leaks.
  - Every helper catches `Exception` from the framework call and returns `False` — never raises past the module boundary so the HTTP layer can trust it.
  - `get_missing_permissions_error()` emits in canonical order (accessibility → screen_recording → input_monitoring) and includes per-permission `how_to_grant` human-readable instructions keyed by the same permission name.
- Added `tests/recorder/test_permissions.py` (24 tests):
  - 8 parametrized combination tests for `check_permissions()` (every boolean triple).
  - Composition tests for `get_missing_permissions_error()`: all granted returns None, single missing, all missing (canonical order), partial missing order preservation.
  - Accessibility helper: framework truthy/falsey, framework raises, framework import broken (ImportError path).
  - Screen Recording helper: preflight True/False, preflight raises → capture-fallback success, preflight raises → capture returns None → False.
  - Input Monitoring helper: tap created → True + invalidate called, tap None → False, tap create raises → False, invalidate raises → True (clean-up failure doesn't taint result).
- Files changed:
  - `services/recorder/src/recorder/permissions.py` (new)
  - `tests/recorder/test_permissions.py` (new)
  - `scripts/ralph/prd.json` (R-004 passes=true)
- Quality gates: `uv run ruff check services/recorder tests/recorder`, `uv run mypy --strict services/recorder`, `uv run pytest tests/recorder/` — all pass (59 tests total).
- **Learnings for future iterations:**
  - `from Module import name` inside a function can be stubbed by installing a `types.ModuleType` into `sys.modules` via `monkeypatch.setitem("Module", stub)`. This is the cleanest way to mock PyObjC without actually touching macOS APIs.
  - To exercise the ImportError path on a local `from X import sym` inside a function, install a stub module that is *missing* `sym`. That raises `ImportError` (not `AttributeError`) because `from-import` specifically surfaces missing names as ImportError.
  - `AXIsProcessTrustedWithOptions` must be invoked with `kAXTrustedCheckOptionPrompt: False` to suppress the system prompt. Tests assert this option is set on the passed dict — do not relax it, or `check_permissions()` could start surfacing the system prompt during polling.
  - The canonical ordering of permissions in `get_missing_permissions_error()` matters for UI consistency — tests lock it in.
  - Ralph's sandbox may have PyObjC installed (on darwin) but not have the permissions granted, so a real call to `_check_input_monitoring()` may legitimately return False there. Do not write tests that assume True without the framework-level mock.
---

## 2026-04-22 - R-005
- Added `services/recorder/src/recorder/event_tap.py` exposing `EventTap` — a passive global CGEventTap running its own `CFRunLoop` on a dedicated `threading.Thread`.
  - `kCGHIDEventTap` + `kCGEventTapOptionListenOnly` (never modifies events).
  - Event mask covers left/right/other mouse down+up, key down+up, flags changed, scroll wheel. Mouse-moved (type 5) is deliberately excluded to keep storage sane.
  - Normaliser `_normalise_event(code, event, quartz)` produces a JSON-friendly dict with `cg_event_type`, `timestamp_ms`, `location_{x,y}`, `modifiers` (decoded from CGEventFlags), `flags_raw`, and per-type `key_code` / `scroll_delta_{x,y}` fields. Each framework call is individually try/except'd so a PyObjC failure on one field does not drop the whole event.
  - `handle_cg_event(code, event)` detects `kCGEventTapDisabledByTimeout` / `kCGEventTapDisabledByUserInput` (both unsigned 0xFFFFFFFE/0xFFFFFFFF and signed -2/-1 forms) and re-arms via `CGEventTapEnable(tap, True)`, surfacing a synthetic `{"cg_event_type": "tap_reenabled", "reason": ...}` dict through the callback.
  - `start(start_timeout=5.0)` blocks until the tap thread has created+enabled the tap (or fails with a clear `RuntimeError` — permission-denied case when `CGEventTapCreate` returns NULL is called out in the error string). `stop()` disables the tap, calls `CFRunLoopStop`, and joins with a 2s timeout; idempotent.
  - Consumer callback exceptions are logged and swallowed — one buggy consumer never tears down capture (belt-and-suspenders for the session orchestrator).
- Added `tests/recorder/test_event_tap.py` (17 hermetic tests + 1 opt-in macOS integration test):
  - Event mask coverage assertions.
  - `_modifiers_from_flags` empty / combined / unknown-bits-ignored.
  - `_normalise_event` for click / key_down / scroll / unknown type / framework-raises.
  - `handle_cg_event` delivery, reenable-on-timeout, reenable-on-user-input (signed form), suppression while stopping, callback-exception isolation.
  - `start/stop` round trip via a stubbed Quartz whose `CFRunLoopRun` blocks on a `threading.Event` until `CFRunLoopStop` sets it.
  - Start-twice raises; tap-create returning NULL raises a permission-referencing RuntimeError.
  - `@pytest.mark.macos test_macos_real_tap_receives_synthesised_click` posts a synthetic CGEvent and asserts delivery (skipped by default; opt-in via `TRACE_RUN_MACOS_TESTS=1`).
- Added `tests/recorder/conftest.py` auto-skip for the `macos` marker unless `TRACE_RUN_MACOS_TESTS=1`, and `tests/recorder/README.md` documenting how a human grants Accessibility / Input Monitoring / Screen Recording permissions and runs the macOS tests.
- Registered the `macos` marker in root `pyproject.toml` so pytest does not warn on unknown markers.
- Files changed:
  - `services/recorder/src/recorder/event_tap.py` (new)
  - `tests/recorder/test_event_tap.py` (new)
  - `tests/recorder/conftest.py` (added macOS skip hook)
  - `tests/recorder/README.md` (new)
  - `pyproject.toml` (macos marker declaration)
  - `scripts/ralph/prd.json` (R-005 passes=true)
- Quality gates: `uv run ruff check services/recorder tests/recorder`, `uv run mypy --strict services/recorder`, `uv run pytest tests/recorder/` — all pass (76 passed, 1 skipped — the opt-in macOS test).
- **Learnings for future iterations:**
  - CoreGraphics constants like `kCGEventTapDisabledByTimeout` are declared `uint32_t` but PyObjC may surface them as signed ints on some macOS/PyObjC combinations. Match both `0xFFFFFFFE` and `-2` (and `0xFFFFFFFF` / `-1`). Same trick will apply to any CoreFoundation constant where PyObjC's type coercion is surprising.
  - A CGEventTap is inert until `CGEventTapEnable(tap, True)` runs AND the tap's run-loop source is attached to a live CFRunLoop. `CGEventTapCreate` returning non-NULL does not mean events will flow. Tests that just check the return value are not enough; the run-loop must be spun.
  - Running `CFRunLoopRun()` on a non-main thread works for event taps (we do not need the main CFRunLoop). `CFRunLoopGetCurrent()` returns the per-thread loop; attaching the tap source there is what routes events.
  - In the Ralph sandbox, `CGEventPost` does not deliver through the tap even when the tap reports "enabled" — so a real-delivery integration test must be opt-in (TRACE_RUN_MACOS_TESTS=1). Attempting to assert delivery in Ralph will always fail.
  - When stubbing Quartz for `start()`/`stop()`, you must provide: `CGEventTapCreate`, `CFMachPortCreateRunLoopSource`, `CFRunLoopGetCurrent`, `CFRunLoopAddSource`, `CFRunLoopRemoveSource`, `CGEventTapEnable`, `CFRunLoopRun` (blocking), `CFRunLoopStop` (releases the block), plus the `kCGHIDEventTap` / `kCGHeadInsertEventTap` / `kCGEventTapOptionListenOnly` / `kCFRunLoopCommonModes` sentinels. See `_install_runloop_stub_quartz` in test_event_tap.py for the pattern — reuse when adding CFRunLoop-driven modules (focus_tracker NSWorkspace notifications, screen-capture streams).
  - Mouse-moved events (CGEventType 5) are intentionally excluded from the mask. If a future story needs drag-path fidelity, add it there and document the storage cost; do not silently re-enable.
---

## 2026-04-22 - R-006
- Added `services/recorder/src/recorder/ax_resolver.py` exposing `resolve_element_at(x, y, *, timeout_seconds=0.2) -> ResolvedTarget | None`.
  - Runs the AX work on a short-lived daemon thread and blocks with `threading.Event.wait(timeout)` so a hung AX server can never hang the caller for more than the timeout. Returns `None` on timeout with a warning log.
  - `_resolve_element_at_sync` calls `AXUIElementCreateSystemWide()` + `AXUIElementCopyElementAtPosition(system_wide, x, y, None)` → `(err, element)`; any non-zero error or null element returns None.
  - `_build_target` collects role / title / description / identifier / position+size. If the hit has no title AND no description, `_walk_for_label` walks up to `MAX_ANCESTOR_DEPTH=3` parents via `AXParent` and adopts the first ancestor label (description too if co-located). The hit's own role / ax_identifier / frame are preserved — only the name is promoted.
  - `_get_frame` tolerates three PyObjC value shapes: bridged tuple `(x, y)`, object with `.x/.y` or `.width/.height`, or raw `AXValueRef` unwrapped via `AXValueGetValue(value, kAXValueCG{Point,Size}Type, None) -> (success, unpacked)`.
  - Every framework call is individually try/except'd; AX errors never escape the module (AC "never raises past the module boundary").
  - `ResolvedTarget` TypedDict is schema-compatible — `frame` required, other fields nullable. If position+size extraction fails, the whole target is reported as None (AC "frame is always present when a target is returned").
- Added `tests/recorder/test_ax_resolver.py` (16 hermetic tests + 1 opt-in macOS integration):
  - Happy-path structured return, miss → None, AX error code → None, missing frame → None, ImportError on ApplicationServices → None.
  - Missing optional fields become `None` (not empty string) in the returned dict.
  - Ancestor walk: promotes parent label at depth-1, stops exactly at MAX_ANCESTOR_DEPTH=3, skipped when hit already has a label, skipped when hit has only a description.
  - Timeout: a slow `AXUIElementCreateSystemWide` stub that sleeps 0.5s is cancelled inside a 0.05s budget.
  - Error isolation: framework-raises and attribute-copy-raises return None cleanly.
  - Frame unpacking: attribute-bearing objects (`.x/.y` / `.width/.height`), AXValueRef+AXValueGetValue `(True, unpacked)` path.
  - `@pytest.mark.macos test_macos_real_resolve_hits_finder_menu_bar` resolves screen (2, 2) — the Apple menu — on a real Mac with Accessibility granted.
- Files changed:
  - `services/recorder/src/recorder/ax_resolver.py` (new)
  - `tests/recorder/test_ax_resolver.py` (new)
  - `scripts/ralph/prd.json` (R-006 passes=true)
- Quality gates: `uv run ruff check services/recorder tests/recorder`, `uv run mypy --strict services/recorder`, `uv run pytest tests/recorder/` — all pass (92 passed, 2 skipped — the opt-in macOS tests for event_tap + ax_resolver).
- **Learnings for future iterations:**
  - `AXUIElementCopyAttributeValue(element, attr, None)` returns `(error, value)` in PyObjC. Always check error != 0 OR value is None — both mean "not available". Tests should model the same convention so the code path for "attribute missing" is actually exercised.
  - PyObjC may surface CGPoint/CGSize as either a native tuple, a bridged object with `.x`/`.y` / `.width`/`.height`, or as an opaque `AXValueRef` that needs `AXValueGetValue(value, kAXValueCGPointType, None)` — which itself returns `(success, unpacked)` on PyObjC. Handle all three shapes; real-world AX responses differ per macOS version and per target app.
  - Bounded-time AX work: `threading.Thread` + `Event.wait(timeout)` is sufficient and simpler than `concurrent.futures` for fire-and-forget short calls. The thread is daemon, so if it stays stuck on a misbehaving AX server, it just dies with the process instead of leaking. Do NOT use `thread.join(timeout)` alone as the signal — on timeout the returned thread is still alive and you cannot distinguish "finished" from "dead" without a separate sentinel. Use a `threading.Event` set in a `finally` block.
  - The resolver preserves the HIT element's role/identifier/frame and only borrows the parent's label when walking — the click coordinate's semantic position should not change just because we found a nicer name.
  - When adopting `str()` on an AX value, handle empty strings: they should be treated as "unknown" (returned as None). AX often reports empty strings rather than nil for missing titles, and passing an empty label downstream confuses synthesis prompts.
---

## 2026-04-22 - R-007
- Added `services/recorder/src/recorder/focus_tracker.py` exposing `FocusTracker` plus `AppInfo`, `AppFocusHistoryEntry`, `AppSwitchPayload`, `WindowFocusPayload` TypedDicts.
  - `start()` subscribes to `NSWorkspaceDidActivateApplicationNotification` via `notificationCenter().addObserverForName_object_queue_usingBlock_(…, self._ns_workspace_block)` and seeds current state from `NSWorkspace.frontmostApplication()` if available. Raises on double-start; `stop()` is idempotent and calls `removeObserver_` with the handle returned from subscribe.
  - `handle_app_activated(app)` is the public seam tests/orchestrator use to drive state without going through a notification; the block path converts the NSNotification userInfo → `AppInfo` dict and calls into the same state machine.
  - Window title resolved via `AXUIElementCreateApplication(pid)` + `AXFocusedWindow` + `AXTitle`. Only queried on app-change transitions and on explicit `refresh_window_title()` — never on a timer, matching the PRD note. Empty titles are reported as `None`.
  - Callbacks (`on_app_switch`, `on_window_focus_change`) fire outside the state lock; consumer exceptions are logged and swallowed so one bad listener never tears down tracking.
  - Maintains `app_focus_history` with `entered_at` / `exited_at` ISO timestamps — `stop()` finalises the last entry. History is returned as a defensive copy so callers can't mutate internal state.
- Added `tests/recorder/test_focus_tracker.py` (24 hermetic + 1 opt-in macOS integration):
  - State machine: first-activation-from-none, subsequent switches carry `from_*`, reactivation same app is a no-op, history entries finalised in order, `stop()` finalises the tail, callback exception is isolated.
  - Window title: refresh only fires on change, None-title doesn't spam callbacks, refresh-before-activation is safe, AX raises / ImportError / create-returns-None all degrade to `None`.
  - NSWorkspace plumbing: `start()` adds exactly one observer for the right notification name, `stop()` removes it, `start()` twice raises, AppKit ImportError keeps the tracker usable via `handle_app_activated`, stored block delivers via a fake notification, missing userInfo is a safe no-op, `frontmostApplication()` seeds state at start.
  - Helper coverage: `_extract_app_info` via both `.get` and `objectForKey_` paths; `_ns_running_app_to_dict` rejects missing pid and converts empty fields to empty strings.
  - `@pytest.mark.macos test_macos_real_app_switch_fires_callback` drives two real apps via `osascript` on a real Mac; skipped by default.
- Files changed:
  - `services/recorder/src/recorder/focus_tracker.py` (new)
  - `tests/recorder/test_focus_tracker.py` (new)
  - `scripts/ralph/prd.json` (R-007 passes=true)
  - `scripts/ralph/progress.txt` (added NSWorkspace + NSDictionary patterns to Codebase Patterns)
- Quality gates: `uv run ruff check services/recorder tests/recorder`, `uv run mypy --strict services/recorder`, `uv run pytest tests/recorder/` — all pass (116 passed, 3 skipped — the opt-in macOS tests for event_tap, ax_resolver, focus_tracker).
- **Learnings for future iterations:**
  - `addObserverForName_object_queue_usingBlock_` is friendlier than the selector-based pattern for PyObjC interop — the block is just a Python callable and PyObjC handles the bridging. Use this whenever you need to observe an `NSNotificationCenter` without introducing an NSObject subclass.
  - NSDictionary bridging is not guaranteed: some PyObjC versions bridge `userInfo()` to a plain dict, others hand back an NSDictionary with only Objective-C accessors. Check for `.get` first (bridged) and fall back to `objectForKey_` (ObjC) — see `_extract_app_info`.
  - Empty-string AX values should collapse to `None` at the module boundary. Otherwise downstream code has to repeatedly special-case `""` as "no title available" and it becomes easy to emit a `window_focus` event with a useless empty title.
  - When writing a test stub that needs to emulate "forced ObjC path" (no Python `.get`), do NOT set `user_info.get = None` on a dict subclass — calling a None later raises TypeError inside the caught `except Exception` and the whole extraction silently returns None. Instead define a separate minimal class that exposes only `objectForKey_`.
  - The focus tracker owns `app_focus_history` for metadata.json. The session orchestrator (R-010) should call `focus_tracker.get_app_focus_history()` right before `writer.close()` (which rewrites metadata with stopped_at) so the last entry's `exited_at` is populated. `FocusTracker.stop()` also finalises the tail so it's safe either way, but calling `stop()` before grabbing the history is cleanest.
---
